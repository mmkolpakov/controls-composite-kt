package space.kscience.controls.composite.ports

/**
 * Defines the byte order for multi-byte values.
 */
public enum class ByteOrder {
    /** Most significant byte first. */
    BIG_ENDIAN,

    /** Least significant byte first. */
    LITTLE_ENDIAN
}